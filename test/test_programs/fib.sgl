import 'sgl:io'

function fib(n) {
  if (n < 2) return n;
  return fib(n - 2) + fib(n - 1);
}

var start = (clock());
// output 317811
// output 317811
// output 317811
// output 317811
// output 317811
for (var i = 0; i < 5; i++)
println(fib(28));
println((clock()) - start); // ignore
